---
title: "모의고사 05회"
lang: ko
exam: 5
---

<!--
  practice-exam-05.md 의 한국어판.
  문항은 **순서**로, 보기는 **글머리(a/b/c)** 로 원문과 짝지어진다.
  순서를 바꾸거나 문항을 빼면 원문과 어긋나므로 그대로 두세요.
-->

> [!question] 한 회사가 AWS 플랫폼에서 전자 상거래 사이트를 운영하기 위해 EC2 인스턴스를 사용하고 있다. 사이트를 사용할 수 없게 되면 이 회사는 사이트를 사용할 수 없는 1분마다 상당한 금액을 잃게 된다. 중단 위험을 최소화하기 위해 이 회사가 사용해야 하는 설계 원칙은 무엇인가?
> a) 최소 권한.
> b) 파일럿 라이트.
> c) 내결함성.
> d) 멀티스레딩.
>> [!success]- Answer
>> c) 내결함성.

> [!question] 1년 기간의 예약 인스턴스를 구매하기로 결정했다. 총 할인이 가장 큰 옵션은 무엇인가?
> a) 전체 선결제 예약.
> b) 모든 예약 인스턴스 결제 옵션은 동일한 할인 수준을 제공한다.
> c) 부분 선결제 예약.
> d) 선결제 없는 예약.
>> [!success]- Answer
>> a) 전체 선결제 예약.

> [!question] 클라우드에서 데이터를 보호하는 데 도움이 되도록 AWS가 제공하는 기능은 무엇인가? (2개 선택)
> a) 액세스 제어.
> b) 물리적 MFA 디바이스.
> c) 데이터 암호화.
> d) 무제한 스토리지.
> e) 로드 밸런싱.
>> [!success]- Answer
>> a) 액세스 제어.
>> c) 데이터 암호화.

> [!question] 한 AWS 고객이 Amazon Linux 인스턴스 하나를 2시간 5분 9초 동안, CentOS 인스턴스 하나를 4시간 23분 7초 동안 사용했다. 이 고객은 얼마의 시간에 대해 과금되는가?
> a) Linux 인스턴스는 3시간, CentOS 인스턴스는 5시간.
> b) Linux 인스턴스는 2시간 5분 9초, CentOS 인스턴스는 4시간 23분 7초.
> c) Linux 인스턴스는 2시간 5분 9초, CentOS 인스턴스는 5시간.
> d) Linux 인스턴스는 3시간, CentOS 인스턴스는 4시간 23분 7초.
>> [!success]- Answer
>> c) Linux 인스턴스는 2시간 5분 9초, CentOS 인스턴스는 5시간.

> [!question] 고객이 지원 사례를 프로그래밍 방식으로 관리할 수 있게 해 주는 AWS Support 기능은 무엇인가?
> a) AWS Trusted Advisor.
> b) AWS Operations Support.
> c) AWS Support API.
> d) AWS Personal Health Dashboard.
>> [!success]- Answer
>> c) AWS Support API.

> [!question] 고객이 AWS Identity and Access Management(IAM)와 상호 작용하는 데 사용할 수 있는 방법은 무엇인가? (2개 선택)
> a) AWS CLI.
> b) AWS 보안 그룹.
> c) AWS SDK.
> d) AWS 네트워크 액세스 제어 목록.
> e) AWS CodeCommit.
>> [!success]- Answer
>> a) AWS CLI.
>> c) AWS SDK.

> [!question] 다음 중 AWS Identity and Access Management(IAM) 자격 증명의 유형은 무엇인가? (2개 선택)
> a) AWS 리소스 그룹.
> b) IAM 정책.
> c) IAM 역할.
> d) IAM 사용자.
> e) AWS Organizations.
>> [!success]- Answer
>> c) IAM 역할.
>> d) IAM 사용자.

> [!question] 다음 중 데이터베이스 읽기 작업의 부하를 분산하는 데 도움이 되는 Amazon RDS 기능은 무엇인가?
> a) 데이터베이스 스냅샷.
> b) 다중 AZ 배포.
> c) 자동 백업.
> d) 읽기 전용 복제본.
>> [!success]- Answer
>> d) 읽기 전용 복제본.

> [!question] AWS는 AWS 서비스와 관련된 보안 및 개인 정보 보호 이벤트를 고객에게 어떻게 알리는가?
> a) AWS ACM 서비스를 사용하여.
> b) 보안 게시판을 사용하여.
> c) AWS Management Console을 사용하여.
> d) 규정 준수 리소스를 사용하여.
>> [!success]- Answer
>> b) 보안 게시판을 사용하여.

> [!question] AWS 리소스에 대한 임시 액세스를 부여하는 데 가장 적합한 IAM 엔터티는 무엇인가?
> a) IAM 사용자.
> b) 키 페어.
> c) IAM 역할.
> d) IAM 그룹.
>> [!success]- Answer
>> c) IAM 역할.

> [!question] 한 회사가 단일 EC2 인스턴스에서 호스팅되는 웹 애플리케이션을 보유하고 있으며, 피크 부하 시 CPU 사용률이 100퍼센트에 근접하고 있다. 이 회사는 서버를 수직으로 확장하는 대신 3개의 Amazon EC2 인스턴스를 병렬로 배포하고 3개의 서버에 트래픽을 분산하기로 결정했다. 트래픽을 고르게 분산하기 위해 이 회사가 사용해야 하는 AWS 서비스는 무엇인가?
> a) AWS Global Accelerator.
> b) AWS Application Load Balancer(ALB).
> c) Amazon CloudFront.
> d) Transit VPC.
>> [!success]- Answer
>> b) AWS Application Load Balancer(ALB).

> [!question] 다음 중 사람의 실수를 없애고 AWS 환경을 생성 및 업데이트하는 프로세스를 자동화하는 데 도움이 되는 접근 방식은 무엇인가?
> a) 소프트웨어 테스트 자동화 도구를 사용한다.
> b) AWS CodeDeploy를 사용하여 AWS 환경을 구축하고 자동화한다.
> c) 코드를 사용하여 AWS 인프라를 프로비저닝하고 운영한다.
> d) 모든 애플리케이션을 전용 호스트로 마이그레이션한다.
>> [!success]- Answer
>> c) 코드를 사용하여 AWS 인프라를 프로비저닝하고 운영한다.

> [!question] 한 회사가 무단 액세스로부터 AWS 계정을 더 잘 보호하려고 한다. 이 목표를 달성하기 위해 고객이 사용할 수 있는 옵션은 다음 중 무엇인가?
> a) SDK 또는 CLI를 통해 이루어지는 모든 API 호출을 제한한다.
> b) 회사의 각 부서(개발, QA, 프로덕션)마다 IAM 계정을 하나씩 생성하고 해당 부서의 모든 직원과 공유한다.
> c) 모든 IAM 사용자 액세스에 대해 다중 인증(MFA)을 요구한다.
> d) 로그인 암호를 두 개 설정한다.
>> [!success]- Answer
>> c) 모든 IAM 사용자 액세스에 대해 다중 인증(MFA)을 요구한다.

> [!question] 사용량에 따라 볼륨 할인을 제공하는 AWS 서비스는 무엇인가?
> a) Amazon VPC.
> b) Amazon S3.
> c) Amazon Lightsail.
> d) AWS Cost Explorer.
>> [!success]- Answer
>> b) Amazon S3.

> [!question] AWS 리소스를 배포할 리전을 결정할 때 고려해야 하는 요소는 다음 중 무엇인가? (2개 선택)
> a) AWS 리전의 보안 수준.
> b) 데이터 주권.
> c) 비용.
> d) 계획된 VPC 수.
> e) 회사 위치와의 지리적 근접성.
>> [!success]- Answer
>> b) 데이터 주권.
>> c) 비용.

> [!question] AWS에서 금융 서비스 웹 애플리케이션을 실행하고 있다. 이 애플리케이션은 데이터를 저장하기 위해 MySQL 데이터베이스를 사용한다. 다음 중 빠른 인메모리 캐시에서 정보를 검색할 수 있게 하여 애플리케이션의 성능을 개선하는 AWS 서비스는 무엇인가?
> a) Amazon EFS.
> b) Amazon Neptune.
> c) Amazon ElastiCache.
> d) DAX.
>> [!success]- Answer
>> c) Amazon ElastiCache.

> [!question] EC2 인스턴스에 Auto Scaling 그룹을 사용할 때의 이점은 무엇인가?
> a) Auto Scaling 그룹은 지연 시간을 줄이고 성능을 개선하기 위해 가장 최근 응답을 전 세계 엣지 로케이션에 캐싱한다.
> b) Auto Scaling 그룹은 애플리케이션 가용성과 내결함성을 높이기 위해 여러 가용 영역에서 EC2 인스턴스를 확장한다.
> c) Auto Scaling 그룹은 전 세계 사용자의 지연 시간을 줄이기 위해 여러 리전에 걸쳐 EC2 인스턴스를 확장한다.
> d) Auto Scaling 그룹은 성능을 향상하기 위해 여러 가용 영역에 애플리케이션 트래픽을 분산한다.
>> [!success]- Answer
>> b) Auto Scaling 그룹은 애플리케이션 가용성과 내결함성을 높이기 위해 여러 가용 영역에서 EC2 인스턴스를 확장한다.

> [!question] AWS 인프라와 기존 인프라 간의 TCO 격차는 최근 몇 년 동안 더욱 커졌다. 다음 중 그 이유가 될 수 있는 것은 무엇인가?
> a) AWS는 고객이 자본 지출에 더 많이 투자하도록 돕는다.
> b) AWS는 모든 인프라 운영을 자동화하므로 고객은 인적 자원 비용을 더 많이 절감한다.
> c) AWS는 고객을 위해 클라우드 컴퓨팅 비용을 계속 낮추고 있다.
> d) AWS는 추가 요금 없이 AWS 리소스를 보호한다.
>> [!success]- Answer
>> c) AWS는 고객을 위해 클라우드 컴퓨팅 비용을 계속 낮추고 있다.

> [!question] 다음 중 "클라우드 내부의 보안"을 구현하는 고객 책임의 예는 무엇인가? (2개 선택)
> a) 애플리케이션용 스키마 구축.
> b) 물리적 하드웨어 교체.
> c) 새 하이퍼바이저 생성.
> d) 기반 인프라의 패치 관리.
> e) 파일 시스템 암호화.
>> [!success]- Answer
>> a) 애플리케이션용 스키마 구축.
>> e) 파일 시스템 암호화.

> [!question] 다음 중 고객이 AWS 리소스를 보호하는 데 사용할 수 있는 MFA 디바이스 유형은 무엇인가?
> a) AWS CloudHSM.
> b) U2F 보안 키.
> c) AWS 액세스 키.
> d) AWS 키 페어.
>> [!success]- Answer
>> b) U2F 보안 키.

> [!question] 한 회사가 기존 .NET 애플리케이션을 가능한 한 빨리 AWS에 배포하려고 한다. 이 목표를 달성하기 위해 고객이 사용해야 하는 AWS 서비스는 무엇인가?
> a) Amazon SNS.
> b) AWS Elastic Beanstalk.
> c) AWS Systems Manager.
> d) AWS Trusted Advisor.
>> [!success]- Answer
>> b) AWS Elastic Beanstalk.

> [!question] 다음 중 Amazon EC2 비용을 추정할 때의 요소가 아닌 것은 무엇인가? (2개 선택)
> a) 인스턴스가 실행되는 시간.
> b) 보안 그룹 수.
> c) 할당된 탄력적 IP 주소.
> d) 호스팅 영역 수.
> e) 인스턴스 수.
>> [!success]- Answer
>> b) 보안 그룹 수.
>> d) 호스팅 영역 수.

> [!question] 기업이 온프레미스 스토리지를 비용 효율적인 방식으로 AWS까지 확장하는 데 도움이 되는 AWS 서비스는 무엇인가?
> a) AWS Data Pipeline.
> b) AWS Storage Gateway.
> c) Amazon Aurora.
> d) Amazon EFS.
>> [!success]- Answer
>> b) AWS Storage Gateway.

> [!question] 한 회사가 온라인 클라우드 스토리지 플랫폼을 구축하고 있다. 비용을 최소화하면서 용량을 자동으로 확장할 수 있는 스토리지 서비스가 필요하다. 이러한 요구 사항을 충족하기 위해 이 회사가 사용해야 하는 AWS 스토리지 서비스는 무엇인가?
> a) Amazon Simple Storage Service.
> b) Amazon Elastic Block Store.
> c) Amazon Elastic Container Service.
> d) AWS Storage Gateway.
>> [!success]- Answer
>> a) Amazon Simple Storage Service.

> [!question] 숙련된 시스템 관리자를 팀에 새로 채용했다. 평소와 같이 그가 AWS 서비스와 상호 작용할 수 있도록 새 IAM 사용자를 생성했다. 첫날에 기존의 모든 Amazon EBS 볼륨의 스냅샷을 생성하여 새 Amazon S3 버킷에 저장하도록 요청했다. 그러나 새 팀원은 EBS 스냅샷도 S3 버킷도 생성할 수 없다고 보고했다. 이 간단한 작업을 수행하지 못하게 하는 원인은 무엇인가?
> a) EBS와 S3는 루트 계정 소유자만 액세스할 수 있다.
> b) 시스템 관리자는 새 IAM 계정을 활성화하기 위해 먼저 AWS Support에 문의해야 한다.
> c) 스냅샷을 저장할 공간이 S3에 충분하지 않다.
> d) 모든 신규 사용자에게는 명시적이지 않은 거부가 적용된다.
>> [!success]- Answer
>> d) 모든 신규 사용자에게는 명시적이지 않은 거부가 적용된다.

> [!question] 외부 감사인이 회사 계정의 AWS 리소스에 대한 모든 액세스 로그를 요청하고 있다. 다음 중 감사인에게 요청된 정보를 제공하는 서비스는 무엇인가?
> a) AWS CloudTrail.
> b) Amazon CloudFront.
> c) AWS CloudFormation.
> d) Amazon CloudWatch.
>> [!success]- Answer
>> a) AWS CloudTrail.

> [!question] 다음 중 Amazon Cloud Directory에 관한 설명으로 옳은 것은 무엇인가?
> a) Amazon Cloud Directory를 사용하면 여러 차원에 걸쳐 데이터 계층 구조를 구성할 수 있다.
> b) Amazon Cloud Directory를 사용하면 비디오 및 데이터 스트림을 실시간으로 분석할 수 있다.
> c) Amazon Cloud Directory를 사용하면 사용자가 기존 Active Directory 자격 증명으로 AWS에 액세스할 수 있다.
> d) Amazon Cloud Directory를 사용하면 도메인 이름을 등록하고 관리할 수 있다.
>> [!success]- Answer
>> a) Amazon Cloud Directory를 사용하면 여러 차원에 걸쳐 데이터 계층 구조를 구성할 수 있다.

> [!question] 한 사용자가 프로덕션 시스템 중단 후 AWS Support의 도움을 받기 위해 "프로덕션 시스템 다운" 지원 사례를 열었다. 이 유형의 지원 사례에 대한 예상 응답 시간은 얼마인가?
> a) 12시간.
> b) 15분.
> c) 24시간.
> d) 1시간.
>> [!success]- Answer
>> d) 1시간.

> [!question] 다음 중 AWS의 애플리케이션을 고가용성으로 만들기 위한 모범 사례는 무엇인가?
> a) 최소 두 개의 가용 영역에 애플리케이션을 배포한다.
> b) 여러 AWS 리전에 걸쳐 Elastic Load Balancing(ELB)을 사용한다.
> c) 동일한 가용 영역의 최소 두 대의 서버에 애플리케이션 코드를 배포한다.
> d) 들어오는 모든 요청을 처리하도록 애플리케이션 코드를 다시 작성한다.
>> [!success]- Answer
>> a) 최소 두 개의 가용 영역에 애플리케이션을 배포한다.

> [!question] AWS와 온프레미스에서 애플리케이션을 실행하는 비용에 관해 TCO 분석을 수행할 때 고려해야 하는 것은 다음 중 무엇인가? (2개 선택)
> a) 인건비 및 IT 비용.
> b) 냉각 및 전력 소비.
> c) Amazon EBS 컴퓨팅 성능.
> d) 소프트웨어 아키텍처.
> e) 소프트웨어 호환성.
>> [!success]- Answer
>> a) 인건비 및 IT 비용.
>> b) 냉각 및 전력 소비.

> [!question] 회사는 AWS에서 호스팅되는 비즈니스 크리티컬 시스템이 중단될 경우 해당 시스템에 관한 지원 문의에 대해 15분 미만의 응답 시간을 요구한다. 이 회사가 사용해야 하는 AWS Support 플랜은 무엇인가?
> a) AWS Basic Support.
> b) AWS Developer Support.
> c) AWS Business Support.
> d) AWS Enterprise Support.
>> [!success]- Answer
>> d) AWS Enterprise Support.

> [!question] 다음 중 서버리스 서비스인 AWS 제품은 무엇인가? (2개 선택)
> a) Amazon EC2.
> b) AWS Lambda.
> c) Amazon DynamoDB.
> d) Amazon EMR.
> e) Amazon RDS.
>> [!success]- Answer
>> b) AWS Lambda.
>> c) Amazon DynamoDB.

> [!question] SSL/TLS 인증서를 신속하게 구매하고 배포할 수 있게 해 주는 AWS 서비스는 무엇인가?
> a) Amazon GuardDuty.
> b) AWS ACM.
> c) Amazon Detective.
> d) AWS WAF.
>> [!success]- Answer
>> b) AWS ACM.

> [!question] EC2 인스턴스의 구성을 자동화하기 위해 Chef와의 통합을 제공하는 AWS 서비스는 무엇인가?
> a) AWS Config.
> b) AWS OpsWorks.
> c) AutoScaling.
> d) AWS CloudFormation.
>> [!success]- Answer
>> b) AWS OpsWorks.

> [!question] 한 고객이 AWS 환경에 객체를 저장하고 해당 객체를 인터넷을 통해 다운로드할 수 있게 하려고 한다. 이를 달성하는 데 사용할 수 있는 AWS 서비스는 무엇인가?
> a) Amazon EBS.
> b) Amazon EFS.
> c) Amazon S3.
> d) Amazon Instance Store.
>> [!success]- Answer
>> c) Amazon S3.

> [!question] 다음 중 Amazon CloudFront로 전달되는 HTTP 및 HTTPS 요청을 모니터링하는 데 사용할 수 있는 서비스는 무엇인가?
> a) AWS WAF.
> b) Amazon CloudWatch.
> c) AWS Cloud9.
> d) AWS CloudTrail.
>> [!success]- Answer
>> b) Amazon CloudWatch.

> [!question] 한 회사가 웹 애플리케이션을 AWS로 마이그레이션하고 있다. 이 애플리케이션의 컴퓨팅 용량은 연중 지속적으로 사용된다. 다음 중 이 회사에 가장 비용 효율적인 솔루션을 제공하는 옵션은 무엇인가?
> a) 온디맨드 인스턴스.
> b) Dedicated Hosts.
> c) 스팟 인스턴스.
> d) 예약 인스턴스.
>> [!success]- Answer
>> d) 예약 인스턴스.

> [!question] 한 회사가 신규 직원에게 Amazon DynamoDB 데이터베이스를 관리할 수 있는 장기 액세스 권한을 부여하려고 한다. 이러한 권한을 부여할 때 권장되는 모범 사례는 다음 중 무엇인가?
> a) IAM 역할을 생성하고 Amazon DynamoDB 액세스 권한이 있는 정책을 연결한다.
> b) IAM 역할을 생성하고 관리자 액세스 권한이 있는 정책을 연결한다.
> c) IAM 사용자를 생성하고 Amazon DynamoDB 액세스 권한이 있는 정책을 연결한다.
> d) IAM 사용자를 생성하고 관리자 액세스 권한이 있는 정책을 연결한다.
>> [!success]- Answer
>> c) IAM 사용자를 생성하고 Amazon DynamoDB 액세스 권한이 있는 정책을 연결한다.

> [!question] Amazon EC2 인스턴스에서 실행되는 애플리케이션에 권한을 부여할 때 모범 사례로 간주되는 것은 다음 중 무엇인가?
> a) 권한을 위임할 때마다 새 IAM 액세스 키를 생성한다.
> b) 필요한 AWS 자격 증명을 애플리케이션 코드 안에 직접 저장한다.
> c) 장기 액세스 키 대신 임시 보안 자격 증명(IAM 역할)을 사용한다.
> d) 아무것도 하지 않는다. Amazon EC2 인스턴스에서 실행되는 애플리케이션은 다른 AWS 서비스나 리소스와 상호 작용하는 데 권한이 필요하지 않다.
>> [!success]- Answer
>> c) 장기 액세스 키 대신 임시 보안 자격 증명(IAM 역할)을 사용한다.

> [!question] 다음 중 AWS 고객이 워크로드를 AWS로 마이그레이션할 때 비용을 절감하는 데 도움이 되는 것은 무엇인가?
> a) 관리형 서비스 대신 서버를 사용한다.
> b) AWS에서 기존 서드 파티 소프트웨어 라이선스를 사용한다.
> c) 프로덕션 워크로드를 AWS 리전 대신 AWS 엣지 로케이션으로 마이그레이션한다.
> d) AWS Outposts를 사용하여 모든 워크로드를 비용 최적화된 환경에서 실행한다.
>> [!success]- Answer
>> b) AWS에서 기존 서드 파티 소프트웨어 라이선스를 사용한다.

> [!question] 한 조직이 모놀리식 기반 아키텍처로 설계된 레거시 애플리케이션을 보유하고 있다. 애플리케이션의 구성 요소를 분리하는 데 사용할 수 있는 AWS 서비스는 무엇인가?
> a) Amazon SQS.
> b) Virtual Private Gateway.
> c) AWS Artifact.
> d) Amazon CloudFront.
>> [!success]- Answer
>> a) Amazon SQS.

> [!question] 다음 중 가상 다중 인증을 활성화하는 데 사용할 수 있는 것은 무엇인가? (2개 선택)
> a) Amazon Connect.
> b) AWS CLI.
> c) AWS Identity and Access Management(IAM).
> d) Amazon SNS.
> e) Amazon Virtual Private Cloud.
>> [!success]- Answer
>> b) AWS CLI.
>> c) AWS Identity and Access Management(IAM).

> [!question] 모범 사례에 따를 때, 다음 중 다수의 바이너리 파일을 처리하는 데 가장 적합한 옵션은 무엇인가?
> a) EC2 인스턴스를 수직으로 확장한다.
> b) RDS 인스턴스를 병렬로 실행한다.
> c) RDS 인스턴스를 수직으로 확장한다.
> d) EC2 인스턴스를 병렬로 실행한다.
>> [!success]- Answer
>> d) EC2 인스턴스를 병렬로 실행한다.

> [!question] 한 회사가 동영상 강의를 전 세계에 배포하기 위해 Amazon S3와 Amazon CloudFront를 사용할 계획이다. 이 회사가 이러한 서비스의 비용을 추정하는 데 사용할 수 있는 도구는 무엇인가?
> a) AWS Cost Explorer.
> b) AWS Pricing Calculator.
> c) AWS Budgets.
> d) AWS Cost & Usage Report.
>> [!success]- Answer
>> b) AWS Pricing Calculator.

> [!question] AWS Management Console에서 생성한 기억이 없는 리소스가 보이면 어떻게 해야 하는가? (2개 선택)
> a) 실행 중인 모든 서비스를 중지하고 조사를 시작한다.
> b) AWS Support가 문제 해결과 계정 보안을 지원할 수 있도록 루트 계정 암호를 AWS Support에 제공한다.
> c) AWS CloudTrail 로그를 확인하고 리소스에 액세스할 수 있는 모든 IAM 사용자를 삭제한다.
> d) 조사를 시작하고 손상되었을 가능성이 있는 IAM 사용자를 삭제한다.
> e) AWS 루트 계정 암호와 모든 IAM 사용자의 암호를 변경한다.
>> [!success]- Answer
>> d) 조사를 시작하고 손상되었을 가능성이 있는 IAM 사용자를 삭제한다.
>> e) AWS 루트 계정 암호와 모든 IAM 사용자의 암호를 변경한다.

> [!question] AWS에서 솔루션을 설계할 때의 핵심 관행은 단일 구성 요소의 장애가 다른 구성 요소에 영향을 주지 않도록 구성 요소 간의 종속성을 최소화하는 것이다. 이 관행을 무엇이라고 하는가?
> a) 탄력적 결합.
> b) 느슨한 결합.
> c) 확장 가능한 결합.
> d) 강한 결합.
>> [!success]- Answer
>> b) 느슨한 결합.

> [!question] 여러 EC2 인스턴스에서 동시에 마운트할 수 있는 NFS 파일 시스템을 제공하는 AWS 서비스는 무엇인가?
> a) Amazon Elastic File System.
> b) Amazon Simple Storage Service.
> c) Amazon Elastic Block Store.
> d) AWS Storage Gateway.
>> [!success]- Answer
>> a) Amazon Elastic File System.

> [!question] 리전 내의 가용 영역은 짧은 지연 시간의 링크로 연결되어 있다. 다음 중 이러한 링크의 이점은 무엇인가?
> a) 데이터 센터에 대한 프라이빗 연결을 생성한다.
> b) 글로벌 고가용성을 달성한다.
> c) 새 컴퓨팅 리소스를 프로비저닝하는 프로세스를 자동화한다.
> d) 데이터의 동기식 복제를 가능하게 한다.
>> [!success]- Answer
>> d) 데이터의 동기식 복제를 가능하게 한다.

> [!question] AWS Lambda에서 지원되는 언어에 관한 설명 중 옳은 것은 무엇인가? (2개 선택)
> a) Lambda는 Python과 Node.js만 지원하지만, 다른 언어의 코드를 이러한 형식으로 변환하는 서드 파티 플러그인을 사용할 수 있다.
> b) Lambda는 Node.js, Python, Java와 같은 다수의 프로그래밍 언어를 기본적으로 지원한다.
> c) Lambda는 마이크로서비스를 위한 AWS 독점 프로그래밍 언어이다.
> d) Lambda는 프로그래밍 언어를 지원하지 않는다. 이는 서버리스 컴퓨팅 서비스이다.
> e) Lambda는 API를 사용하여 모든 프로그래밍 언어를 지원할 수 있다.
>> [!success]- Answer
>> b) Lambda는 Node.js, Python, Java와 같은 다수의 프로그래밍 언어를 기본적으로 지원한다.
>> e) Lambda는 API를 사용하여 모든 프로그래밍 언어를 지원할 수 있다.

> [!question] AWS X-Ray의 기능은 무엇인가? (2개 선택)
> a) 애플리케이션 구성 요소를 자동으로 분리한다.
> b) 애플리케이션 문제를 식별하기 위해 사용자 요청을 추적할 수 있게 해 준다.
> c) 애플리케이션 성능을 개선하는 데 도움이 된다.
> d) Amazon EC2 인스턴스에 애플리케이션을 배포한다.
> e) 온프레미스 서버에 애플리케이션을 배포한다.
>> [!success]- Answer
>> b) 애플리케이션 문제를 식별하기 위해 사용자 요청을 추적할 수 있게 해 준다.
>> c) 애플리케이션 성능을 개선하는 데 도움이 된다.
