---
title: "모의고사 07회"
lang: ko
exam: 7
---

<!--
  practice-exam-07.md 의 한국어판.
  문항은 **순서**로, 보기는 **글머리(a/b/c)** 로 원문과 짝지어진다.
  순서를 바꾸거나 문항을 빼면 원문과 어긋나므로 그대로 두세요.
-->

> [!question] 다음 중 Amazon S3의 민감한 데이터를 보호하는 데 도움이 되는 것은 무엇인가? (2개 선택)
> a) 데이터가 암호화되면 암호화 키를 삭제한다.
> b) AWS를 사용하면 암호화에 대해 걱정할 필요가 없다.
> c) S3 암호화를 활성화한다.
> d) 업로드하기 전에 데이터를 암호화한다.
> e) S3에 액세스할 수 있는 모든 IAM 사용자를 삭제한다.
>> [!success]- Answer
>> c) S3 암호화를 활성화한다.
>> d) 업로드하기 전에 데이터를 암호화한다.

> [!question] 개발자가 코드를 컴파일하고 테스트하는 데 도움이 되는 AWS 서비스는 무엇인가?
> a) AWS CodeDeploy.
> b) AWS CodeCommit.
> c) CloudEndure.
> d) AWS CodeBuild.
>> [!success]- Answer
>> d) AWS CodeBuild.

> [!question] 다음 중 S3에 객체를 저장할 때 부과되는 요금에 영향을 주는 것은 무엇인가? (2개 선택)
> a) 임의 개수의 S3 버킷에 기본 암호화를 사용하는 것.
> b) 인스턴스에 연결된 EBS 볼륨의 수.
> c) 저장된 객체에 사용된 스토리지 클래스.
> d) S3 버킷을 생성하고 삭제하는 것.
> e) 저장된 모든 객체의 총 크기(기가바이트).
>> [!success]- Answer
>> c) 저장된 객체에 사용된 스토리지 클래스.
>> e) 저장된 모든 객체의 총 크기(기가바이트).

> [!question] Amazon CloudFront 서비스는 무엇을 제공하는가? (2개 선택)
> a) 사용자 활동과 API 사용을 추적한다.
> b) 엣지에서 캐싱하여 애플리케이션 가용성을 높인다.
> c) 더 빠른 재해 복구를 가능하게 한다.
> d) 아카이브 데이터를 매우 저렴한 비용으로 저장한다.
> e) 낮은 지연 시간으로 최종 사용자에게 콘텐츠를 전달한다.
>> [!success]- Answer
>> b) 엣지에서 캐싱하여 애플리케이션 가용성을 높인다.
>> e) 낮은 지연 시간으로 최종 사용자에게 콘텐츠를 전달한다.

> [!question] 현재 컨택 센터에 많은 문제를 겪고 있다. 고객에게 더 나은 서비스를 제공할 수 있는 클라우드 기반 컨택 센터를 제공하는 서비스는 무엇인가?
> a) Amazon Lightsail.
> b) Amazon Connect.
> c) AWS Direct Connect.
> d) AWS Elastic Beanstalk.
>> [!success]- Answer
>> b) Amazon Connect.

> [!question] 최근 애플리케이션을 AWS로 마이그레이션했다. 계정에 적용된 AWS 비용을 어떻게 확인할 수 있는가?
> a) AWS Cost and Usage Report를 사용하여.
> b) AWS 총 소유 비용(TCO) 대시보드를 사용하여.
> c) AWS CloudWatch 로그 대시보드를 사용하여.
> d) Amazon VPC 대시보드를 사용하여.
>> [!success]- Answer
>> a) AWS Cost and Usage Report를 사용하여.

> [!question] 다음 중 유효한 Amazon EC2 예약 인스턴스 유형은 무엇인가? (2개 선택)
> a) 컨버터블.
> b) 신속 처리.
> c) 대량.
> d) 스팟.
> e) 표준.
>> [!success]- Answer
>> a) 컨버터블.
>> e) 표준.

> [!question] 다음 중 AWS 감사인이 발행한 모든 보고서와 인증에 액세스할 수 있게 해 주는 서비스는 무엇인가?
> a) AWS Artifact.
> b) AWS Config.
> c) Amazon CloudWatch.
> d) AWS CloudTrail.
>> [!success]- Answer
>> a) AWS Artifact.

> [!question] AWS에서 개발, 테스트, 프로덕션이라는 서로 다른 환경을 가진 블로그를 관리하고 있다. 각 환경마다 사용자 지정 콘솔을 만들어 리소스를 쉽게 보고 관리하려면 무엇을 사용할 수 있는가?
> a) AWS 리소스 그룹.
> b) AWS 배치 그룹.
> c) AWS Management Console.
> d) AWS 태그 편집기.
>> [!success]- Answer
>> a) AWS 리소스 그룹.

> [!question] 실행 중인 EC2 인스턴스에서 지표를 수집하는 AWS 서비스는 무엇인가?
> a) Amazon Inspector.
> b) Amazon CloudWatch.
> c) AWS CloudFormation.
> d) AWS CloudTrail.
>> [!success]- Answer
>> b) Amazon CloudWatch.

> [!question] 현재 웹 애플리케이션에 성능 문제가 있고 로드 시간이 길다. 다음 중 이러한 문제를 해결하고 성능을 개선하는 데 도움이 되는 AWS 서비스는 무엇인가?
> a) Amazon Detective.
> b) AWS X-Ray.
> c) AWS Security Hub.
> d) AWS Shield.
>> [!success]- Answer
>> b) AWS X-Ray.

> [!question] 다음 중 서버리스인 컴퓨팅 리소스는 무엇인가? (2개 선택)
> a) Amazon EC2.
> b) AWS Fargate.
> c) AWS Lambda.
> d) Amazon ECS.
> e) Amazon EMR.
>> [!success]- Answer
>> b) AWS Fargate.
>> c) AWS Lambda.

> [!question] 규정 준수 및 규제 목적으로 한 정부 기관은 애플리케이션이 자신들에게만 전용으로 제공되는 하드웨어에서 실행되어야 한다고 요구한다. 이 요구 사항을 어떻게 충족할 수 있는가?
> a) EC2 전용 호스트를 사용한다.
> b) EC2 예약 인스턴스를 사용한다.
> c) EC2 스팟 인스턴스를 사용한다.
> d) EC2 온디맨드 인스턴스를 사용한다.
>> [!success]- Answer
>> a) EC2 전용 호스트를 사용한다.

> [!question] 기존 AWS 리소스를 최대한 활용하고 비용을 줄이기 위해 워크로드를 정기적으로 개선하도록 권장하는 AWS 비용 거버넌스 모범 사례는 무엇인가?
> a) 태그 지정 적용.
> b) 아키텍처 최적화.
> c) 예산 편성 프로세스.
> d) 리소스 제어.
>> [!success]- Answer
>> b) 아키텍처 최적화.

> [!question] 한 조직이 ACID 트랜잭션 지원이 필요한 금융 애플리케이션을 구축해야 한다. 이 경우 가장 적합한 AWS 데이터베이스 서비스는 무엇인가?
> a) RedShift.
> b) RDS.
> c) CloudHSM.
> d) DMS.
>> [!success]- Answer
>> b) RDS.

> [!question] IAM 사용자에게 직접 권한을 할당하려면 무엇을 사용할 수 있는가?
> a) IAM 자격 증명.
> b) IAM 그룹.
> c) IAM 역할.
> d) IAM 정책.
>> [!success]- Answer
>> d) IAM 정책.

> [!question] 한 전자상거래 애플리케이션의 소유자는 컴퓨팅 용량 요구 사항이 시시각각 크게 달라진다는 것을 알게 되었다. 이러한 유형의 애플리케이션에서 AWS가 기존 데이터 센터보다 경제적인 이유는 무엇인가?
> a) AWS를 사용하면 고객이 부하 급증을 처리하기 위해 강력한 EC2 인스턴스를 시작할 수 있다.
> b) AWS를 사용하면 고객이 더 큰 할인을 받기 위해 선결제할 수 있다.
> c) AWS를 사용하면 고객이 수요에 따라 EC2 인스턴스를 시작하고 종료할 수 있다.
> d) AWS를 사용하면 고객이 자신의 요구에 가장 적합한 더 저렴한 유형의 EC2 인스턴스를 선택할 수 있다.
>> [!success]- Answer
>> c) AWS를 사용하면 고객이 수요에 따라 EC2 인스턴스를 시작하고 종료할 수 있다.

> [!question] Amazon RDS는 선택할 수 있는 여러 데이터베이스 엔진을 지원한다. 다음 중 이에 해당하지 않는 것은 무엇인가?
> a) PostgreSQL.
> b) Oracle.
> c) Microsoft SQL Server.
> d) Teradata.
>> [!success]- Answer
>> d) Teradata.

> [!question] 다음 중 온프레미스 데이터베이스를 AWS로 마이그레이션하는 데 도움이 되는 AWS 서비스는 무엇인가?
> a) AWS DMS.
> b) Amazon S3 Transfer Acceleration.
> c) AWS Directory Service.
> d) AWS Transit Gateway.
>> [!success]- Answer
>> a) AWS DMS.

> [!question] AWS를 처음 사용하는 고객에게 AWS에서 간단한 WordPress 웹 사이트를 시작하는 가장 쉬운 방법은 무엇인가?
> a) Amazon Lightsail 인스턴스에서 WordPress를 실행한다.
> b) Amazon EC2 인스턴스에 WordPress를 설치한다.
> c) Amazon S3 웹 호스팅 기능을 사용한다.
> d) AWS Cloud Development Kit(AWS CDK)에서 웹 사이트를 직접 호스팅한다.
>> [!success]- Answer
>> a) Amazon Lightsail 인스턴스에서 WordPress를 실행한다.

> [!question] 다음 중 AWS 클라우드에서 암호화 키를 관리하는 데 사용하는 것은 무엇인가? (2개 선택)
> a) AWS KMS.
> b) AWS Certificate Manager.
> c) AWS CodeDeploy.
> d) AWS CodeCommit.
> e) CloudHSM.
>> [!success]- Answer
>> a) AWS KMS.
>> e) CloudHSM.

> [!question] 다음 중 사용자 지정 관계형 데이터베이스 소프트웨어를 설치하고 실행할 수 있게 해 주는 서비스는 무엇인가?
> a) Amazon EC2.
> b) Amazon Cognito.
> c) Amazon RDS.
> d) Amazon Inspector.
>> [!success]- Answer
>> a) Amazon EC2.

> [!question] 애플리케이션의 CPU 및 RAM 요구 사항이 예측할 수 없는 방식으로 변하고 있다. 부하에 따라 이러한 리소스를 동적으로 조정하는 데 사용할 수 있는 서비스는 무엇인가?
> a) Auto Scaling.
> b) ELB.
> c) Amazon Route53.
> d) Amazon Elastic Container Service.
>> [!success]- Answer
>> a) Auto Scaling.

> [!question] 한 회사가 온프레미스 데이터 센터에 인프라를 호스팅하고 있다. 현재 자격 증명 관리를 담당하는 운영 팀이 있다. AWS 클라우드로 마이그레이션하기로 결정한다면, 다음 중 AWS에서 동일한 역할을 수행하는 데 도움이 되는 서비스는 무엇인가?
> a) AWS IAM.
> b) AWS Outposts.
> c) AWS Federation.
> d) Amazon Redshift.
>> [!success]- Answer
>> a) AWS IAM.

> [!question] 퍼블릭 클라우드 시스템을 설계할 때의 핵심 설계 원칙은 무엇인가? (2개 선택)
> a) 온디맨드 대신 예약 용량.
> b) 긴밀한 결합보다 느슨한 결합.
> c) 관리형 서비스 대신 서버.
> d) 고정된 서버 대신 폐기 가능한 리소스.
> e) 다중 리전 배포 대신 다중 AZ 배포.
>> [!success]- Answer
>> b) 긴밀한 결합보다 느슨한 결합.
>> d) 고정된 서버 대신 폐기 가능한 리소스.

> [!question] AWS 계정 소유자는 자격 증명 상태를 포함하여 계정의 모든 사용자 목록을 어디에서 얻을 수 있는가?
> a) AWS CloudTrail 추적.
> b) IAM 자격 증명 보고서.
> c) AWS Artifact 보고서.
> d) AWS Cost and Usage Report.
>> [!success]- Answer
>> b) IAM 자격 증명 보고서.

> [!question] 다음 중 AWS 클라우드에서 자체 암호화 키를 쉽게 생성하고 사용할 수 있게 해 주는 서비스는 무엇인가?
> a) AWS Shield.
> b) AWS Certificate Manager.
> c) AWS CloudHSM.
> d) AWS WAF.
>> [!success]- Answer
>> c) AWS CloudHSM.

> [!question] 전 세계 사용자를 대상으로 하는 웹 애플리케이션을 개발했다. 다음 중 인프라 관점에서 가장 높은 중복성과 내결함성을 달성하는 데 도움이 되는 것은 무엇인가?
> a) AWS는 기본적으로 중복성을 갖추고 있으므로 AWS에서는 이러한 기능을 위해 아키텍처를 설계할 필요가 없다.
> b) 단일 가용 영역에 애플리케이션을 배포한다.
> c) 단일 AWS 리전의 여러 가용 영역에 애플리케이션을 배포한다.
> d) 여러 AWS 리전의 여러 가용 영역에 애플리케이션을 배포한다.
>> [!success]- Answer
>> d) 여러 AWS 리전의 여러 가용 영역에 애플리케이션을 배포한다.

> [!question] 일부 서비스의 경우 AWS는 서버 장애나 가용 영역 중단 시 내결함성을 제공하기 위해 여러 가용 영역에 데이터를 자동으로 복제한다. 여러 가용 영역에 데이터를 자동으로 복제하는 서비스는 무엇인가? (2개 선택)
> a) Instance Store.
> b) S3.
> c) DynamoDB.
> d) Amazon Route 53.
> e) AWS VPN.
>> [!success]- Answer
>> b) S3.
>> c) DynamoDB.

> [!question] 다음 중 Amazon CloudFront 비용에 영향을 주는 요소는 무엇인가? (2개 선택)
> a) 요청 수.
> b) 트래픽 배포.
> c) 볼륨 수.
> d) 인스턴스 유형.
> e) 스토리지 클래스.
>> [!success]- Answer
>> a) 요청 수.
>> b) 트래픽 배포.

> [!question] 다음 중 AWS가 제공하는 서비스의 금지된 사용에 대해 자세히 알아보기 위해 AWS 고객이 사용할 수 있는 리소스는 무엇인가?
> a) AWS 서비스 제어 정책(SCP).
> b) AWS Artifact.
> c) AWS Budgets.
> d) AWS 이용 목적 제한 방침(AWS Acceptable Use Policy).
>> [!success]- Answer
>> d) AWS 이용 목적 제한 방침(AWS Acceptable Use Policy).

> [!question] 다음 중 모든 사용자가 무료로 사용할 수 있는 보안 리소스는 무엇인가? (2개 선택)
> a) AWS 게시판.
> b) AWS TAM.
> c) AWS Support API.
> d) AWS 보안 블로그.
> e) AWS 강의실 교육.
>> [!success]- Answer
>> a) AWS 게시판.
>> d) AWS 보안 블로그.

> [!question] Amazon S3에 저장된 데이터를 실수로 인한 삭제로부터 어떻게 보호할 수 있는가?
> a) S3 버전 관리를 활성화하여.
> b) S3 버킷 정책을 구성하여.
> c) S3 수명 주기 정책을 구성하여.
> d) S3 교차 리전 복제(CRR)를 비활성화하여.
>> [!success]- Answer
>> a) S3 버전 관리를 활성화하여.

> [!question] AWS 공동 책임 모델에 따를 때 다음 중 AWS의 책임에 해당하는 것은 무엇인가?
> a) 리전과 엣지 로케이션 보호.
> b) 감사 작업 수행.
> c) AWS 리소스 사용량 모니터링.
> d) AWS 리소스에 대한 액세스 보호.
>> [!success]- Answer
>> a) 리전과 엣지 로케이션 보호.

> [!question] 다음 중 7가지 핵심 AWS Trusted Advisor 검사에만 액세스할 수 있는 AWS Support 플랜은 무엇인가?
> a) Business 및 Enterprise Support.
> b) Basic 및 Developer Support.
> c) Developer 및 Enterprise Support.
> d) Developer 및 Business Support.
>> [!success]- Answer
>> b) Basic 및 Developer Support.

> [!question] 다음 중 AWS Lambda를 사용할 때의 이점이 아닌 것은 무엇인가?
> a) AWS Lambda는 서버를 프로비저닝하거나 관리하지 않고 코드를 실행한다.
> b) AWS Lambda는 클라우드에서 크기 조정이 가능한 컴퓨팅 용량을 제공한다.
> c) AWS Lambda 코드가 실행되지 않을 때는 요금이 부과되지 않는다.
> d) AWS Lambda는 모든 모바일 앱에서 직접 호출할 수 있다.
>> [!success]- Answer
>> d) AWS Lambda는 모든 모바일 앱에서 직접 호출할 수 있다.

> [!question] AWS는 고객이 클라우드에서 규정 준수를 달성하는 데 어떻게 도움을 주는가?
> a) 클라우드에서는 규제 준수 요구 사항을 충족할 수 없다.
> b) AWS는 가장 일반적인 클라우드 보안 표준을 적용하며, 고객에게 적용되는 법률과 규정의 준수를 책임진다.
> c) AWS는 ISO 9001, HIPAA와 같은 다수의 일반적인 보증 인증을 보유하고 있다.
> d) 많은 AWS 서비스는 현지 법률과 규정을 준수하기 위해 정기적으로 평가된다.
>> [!success]- Answer
>> c) AWS는 ISO 9001, HIPAA와 같은 다수의 일반적인 보증 인증을 보유하고 있다.

> [!question] AWS 공동 책임 모델에서 DynamoDB 데이터베이스 확장은 누구의 책임인가?
> a) 보안 팀.
> b) 개발 팀.
> c) AWS.
> d) 내부 DevOps 팀.
>> [!success]- Answer
>> c) AWS.

> [!question] 한 개발 팀이 기반 Amazon EC2 인스턴스를 프로비저닝하거나 관리하지 않고 AWS에서 Docker 컨테이너를 실행하려고 한다. 이 요구 사항을 충족하는 AWS 서비스는 무엇인가?
> a) AWS Fargate.
> b) Amazon EC2 Auto Scaling.
> c) AWS Batch.
> d) Amazon Lightsail.
>> [!success]- Answer
>> a) AWS Fargate.

> [!question] AWS Organizations 서비스의 이점은 무엇인가? (2개 선택)
> a) AWS 서비스에 대한 액세스를 제어한다.
> b) 조직이 성공적인 클라우드 도입을 위한 가속화된 경로를 설계하고 유지하는 데 도움이 된다.
> c) 조직의 결제 수단을 관리한다.
> d) 조직이 AWS로 원하는 비즈니스 성과를 달성하는 데 도움이 된다.
> e) 여러 AWS 계정에 걸쳐 결제를 통합한다.
>> [!success]- Answer
>> a) AWS 서비스에 대한 액세스를 제어한다.
>> e) 여러 AWS 계정에 걸쳐 결제를 통합한다.

> [!question] 클라우드에서 데이터 웨어하우스를 구축할 수 있게 해 주는 AWS 서비스는 무엇인가?
> a) AWS Shield.
> b) Amazon Redshift.
> c) Amazon RDS.
> d) Amazon Comprehend.
>> [!success]- Answer
>> b) Amazon Redshift.

> [!question] AWS 리소스에서 실행되는 서드 파티 소프트웨어 솔루션과 서비스를 구매할 수 있게 해 주는 AWS 서비스는 무엇인가?
> a) AWS Application Discovery service.
> b) Amazon DevPay.
> c) AWS Marketplace.
> d) 리소스 그룹.
>> [!success]- Answer
>> c) AWS Marketplace.

> [!question] 다음 중 애플리케이션 코드를 저장, 버전 관리, 관리할 수 있게 해 주는 AWS 리포지토리 관리 시스템은 무엇인가?
> a) AWS CodePipeline.
> b) AWS CodeCommit.
> c) AWS X-Ray.
> d) Amazon Inspector.
>> [!success]- Answer
>> b) AWS CodeCommit.

> [!question] 지연 시간을 줄이기 위해 최종 사용자를 가장 가까운 AWS 리전으로 라우팅하는 데 사용할 수 있는 AWS 서비스는 무엇인가?
> a) Amazon Cognito.
> b) AWS Systems Manager.
> c) AWS Cloud9.
> d) Amazon Route 53.
>> [!success]- Answer
>> d) Amazon Route 53.

> [!question] 사용자가 기존 회사 자격 증명으로 AWS 계정에 로그인할 수 있게 해 주는 기능은 무엇인가?
> a) 페더레이션.
> b) 액세스 키.
> c) IAM 권한.
> d) WAF 규칙.
>> [!success]- Answer
>> a) 페더레이션.

> [!question] AWS 공동 책임 모델에 따를 때, 고객이 AWS로부터 전적으로 상속받는 제어는 무엇인가? (2개 선택)
> a) 인지 및 교육.
> b) 커뮤니케이션 제어.
> c) 데이터 센터 보안 제어.
> d) 환경 제어.
> e) 리소스 구성 관리.
>> [!success]- Answer
>> c) 데이터 센터 보안 제어.
>> d) 환경 제어.

> [!question] https://status.aws.amazon.com/ URL을 방문하면 무엇에 액세스할 수 있는가?
> a) AWS Billing Dashboard.
> b) AWS Cost Dashboard.
> c) AWS Service Health Dashboard.
> d) AWS Security Dashboard.
>> [!success]- Answer
>> c) AWS Service Health Dashboard.

> [!question] 다음 중 최종 사용자가 데이터를 검색할 때 지연 시간을 줄일 수 있는 절차는 무엇인가? (2개 선택)
> a) 최종 사용자와 가장 가까운 리전에 미디어 자산을 저장한다.
> b) 추가 EBS 볼륨에 미디어 자산을 저장하고 서버의 용량을 늘린다.
> c) 미디어 자산을 최소 두 개의 가용 영역에 복제한다.
> d) Amazon Elastic Transcoder를 사용하여 미디어 자산의 크기를 줄인다.
> e) 미디어 자산을 S3에 저장하고 CloudFront를 사용하여 이러한 자산을 배포한다.
>> [!success]- Answer
>> a) 최종 사용자와 가장 가까운 리전에 미디어 자산을 저장한다.
>> e) 미디어 자산을 S3에 저장하고 CloudFront를 사용하여 이러한 자산을 배포한다.

> [!question] 다음 중 클라우드 보안을 위한 7가지 설계 원칙에 속하는 것은 무엇인가? (2개 선택)
> a) 수동 모니터링 기법을 사용하여 AWS 리소스를 보호한다.
> b) 장기 자격 증명 대신 IAM 역할을 사용하여 임시 액세스를 부여한다.
> c) 장애로부터 보호하기 위해 수평으로 확장한다.
> d) 실시간 추적성을 활성화한다.
> e) 민감한 데이터는 절대 클라우드에 저장하지 않는다.
>> [!success]- Answer
>> b) 장기 자격 증명 대신 IAM 역할을 사용하여 임시 액세스를 부여한다.
>> d) 실시간 추적성을 활성화한다.

> [!question] 한 회사가 프로덕션 워크로드를 AWS로 마이그레이션하고 있으며, 여러 부서에 걸친 비용 관리를 우려하고 있다. AWS 지출을 분류하고 추적하기 위해 이 회사가 구현해야 하는 옵션은 무엇인가?
> a) AWS Pricing Calculator 서비스를 사용하여 각 부서에서 발생한 비용을 모니터링한다.
> b) Amazon Aurora를 사용하여 사용량을 기준으로 AWS 지출을 예측한다.
> c) 비용 할당 태그를 적용하여 프로젝트와 부서별로 AWS 비용을 구분한다.
> d) 각 부서에 대한 결제 업데이트를 자동으로 수신하도록 AWS Price List API를 구성한다.
>> [!success]- Answer
>> c) 비용 할당 태그를 적용하여 프로젝트와 부서별로 AWS 비용을 구분한다.
