---
title: "소재 대장"
tags: [saa-c03, 문제은행, 출처]
lang: ko
---

> [!info] 어떤 자료에서 무엇을 뽑아 썼는지 기록
> 문항을 만들 때마다 여기에 한 줄 남깁니다. **같은 자료를 두 번 캐거나 빠뜨리지 않으려고** 둡니다.
> 우리 문항의 출처·방식은 [참고 자료](/docs/references) 쪽에 있고, 이 파일은 **작업 기록**입니다.

## 원칙

원문(지문·보기 문장)은 이 저장소에 두지 않습니다. 가져오는 것은 **판단이 갈리는 지점**뿐이고,
상황·보기·해설은 새로 씁니다. 그래서 대장에도 원문이 아니라 **판단 포인트의 이름**만 적습니다.

원본 파일은 작업자 로컬에 있습니다. 다시 캐야 하면 아래 위치에서 꺼내세요.

## 소재

| # | 자료 | 범위 | 원본 문항 | 소화 | 상태 |
|---|---|---|---|---|---|
| 1 | velog `@gagaeun` — Examtopics 헷갈리는 문제 정리 | 글 제목은 `1-544` 지만 실제 정리된 것은 **257개** | 257 | 257 | **완료** — 판단 포인트 전부 소화 |
| 2 | 대화로 받은 ExamTopics 문항 (#1~40) | 40 | 40 | 40 | **완료** — 27개는 소재 1과 중복, 13개만 새로 반영 |
| 3 | `Downloads/quiz.md` (#41~140) | 제목의 140은 마지막 번호, 실제 **100개** | 100 | 100 | **완료** — 대부분 중복, 새 판단 포인트만 반영 |
| 4 | `Downloads/quiz.md` 교체본 (#141~260) | 실제 **120개** | 120 | 120 | **완료** — 소재 3보다 중복이 더 많음. 복원력·비용 신규만 반영 |

> [!warning] 개수는 제목이 아니라 실물을 세세요
> 소재 1은 제목이 `1-544` 라 544개로 보이지만 257개였고, 소재 3은 마지막 번호가 140이라
> 140개로 보이지만 100개였습니다. **번호 범위와 개수는 다릅니다.**
> 확인은 `grep -c` 로 마커를 직접 세는 것이 가장 빠릅니다.

## 소재 1·2·3 에서 반복된 것

세 자료가 같은 문제 세트를 다른 각도로 정리한 것이라 겹침이 많았습니다.
아래는 **세 곳 모두에 나온** 판단 포인트입니다. 이미 문항으로 들어가 있으니 또 만들지 마세요.

`aws:PrincipalOrgID` · EBS→EFS 공유 · 70TB Snowball · SMB File Gateway 7일 · S3 게이트웨이 엔드포인트 ·
EC2 IAM 역할 · Gateway Load Balancer · SQS FIFO 순서 · CloudFront 정적+동적 · Network Firewall vs
Firewall Manager · Session Manager · ALB HTTP→HTTPS 리디렉트 · Lambda 동시성→SQS · Shield Advanced ·
CloudWatch 대시보드 공유 · Direct Connect + VPN 백업

## 커버리지 점검으로 찾은 갭 (2026-08-17)

소재 3의 서비스·기능 용어 63개를 문제은행 전체와 대조했더니 **55개는 이미 다루고** 있었고,
실제로 비어 있던 것은 셋이었습니다. 셋 다 채웠습니다.

| 갭 | 왜 비어 있었나 |
|---|---|
| S3 Glacier Instant Retrieval | 계층 문항이 Standard-IA 와 Deep Archive 만 다뤄 **분기 1회 즉시 조회** 구간이 없었음 |
| MFA 삭제 | 버전 관리·객체 잠금은 있는데 이것만 빠져 있었음 |
| 요청자 지불(Requester Pays) | 데이터 전송 비용 문항에 없었음 |

처음에는 Run Command · Data Pipeline · NAT 인스턴스 · ACM 을 "오답 보기로만 등장한다"며 넘겼는데,
그건 판단이 틀렸습니다. **오답 보기로 자주 나오는 서비스야말로 자신 있게 지울 줄 알아야 하는 대상**입니다.
Run Command 와 NAT 인스턴스는 각각 정답이 되는 상황이 따로 있어 문항으로 만들었고, ACM 은 이미
세 문항에서 다루고 있었습니다(영문 용어로 대조하느라 놓쳤던 오탐).

**Data Pipeline 만 만들지 않았습니다.** AWS 가 물러나게 한 서비스라 정답이 되는 문항을 쓰면
지금 시험에 틀린 것을 가르치게 됩니다. 오답 보기로 나오면 "요즘은 Glue·Step Functions" 로 지우면 됩니다.

> 이 점검은 다시 돌릴 수 있습니다 — 소재의 용어를 뽑아 `practice-exam-*.md` 와 `_inbox*.md` 전체에
> `grep -i` 로 대조하면 됩니다. 한글 표기로 쓴 개념은 영문 용어로 안 잡히니 대조표를 같이 두세요.

## 소재 없이 만든 회차 (2026-08-17)

5회차는 새 자료를 캐서 만든 것이 아닙니다. **이미 있던 D2·D4 문항 103개를 전부 훑어
비어 있는 갈림길을 찾아** 13문항을 새로 썼습니다.

소재가 알려 주는 것은 "시험에 어떤 갈림길이 나오는가" 뿐이고, 그건 200문항쯤 쌓이면
이미 대부분 알고 있습니다. 그때부터는 **새 소재를 기다리는 것보다 빈 곳을 찾는 쪽이 빠릅니다.**

이때 채운 자리 — ASG 상태 확인을 EC2 에서 ELB 로 · DLQ 최대 수신 횟수 · Aurora 백트랙 ·
ElastiCache Redis 다중 AZ · 읽기 복제본 승격 · 인스턴스 새로 고침 · 복제 시간 제어(RTC) ·
Step Functions 재시도·분기 · 예열 대기 · NAT 인스턴스 트레이드오프 · 미사용 탄력적 IP 요금 ·
불완전한 멀티파트 업로드 · Run Command

> 빈 곳을 찾는 방법: `모듈 [[..]]` 태그로 도메인을 걸러 **정답 줄만 뽑아 정렬**하면
> 무엇을 이미 묻고 있는지 한 화면에 들어옵니다. 겹치지 않는 자리가 곧 다음 문항입니다.

## 다음에 캘 때

1. 먼저 이 대장에서 **이미 완료된 소재인지** 본다
2. 새 소재면 `grep -c` 로 실제 개수를 센다
3. 판단 포인트를 뽑고 **기존 문항과 겹치는지** 대조한다 (`grep -li` 로 핵심어 검색)
4. 새 것만 `_inbox.md` 에 우리 문항으로 쓴다
5. `node scripts/rebalance-quiz.mjs` 로 편성한다
6. 이 대장에 한 줄 남긴다

> [!important] 도메인 쏠림을 먼저 보세요
> 소재 1·2·3 이 전부 **보안·고성능에 치우쳐** 있었습니다. 그대로 캐면 회차를 못 만듭니다.
> `rebalance-quiz.mjs` 가 마지막에 찍는 **주문서**(도메인별 부족분)를 보고,
> 그 도메인에 해당하는 문항만 골라 캐는 편이 훨씬 빠릅니다.
