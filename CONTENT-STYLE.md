# 콘텐츠 작성 규칙

`content/docs/aws-clf-c02/` 아래 문서를 쓸 때 지키는 규칙.
완성된 예시는 **`20-course/01-cloud-intro.md`** 와 **`20-course/02-cloud-computing.md`** 이다. 쓰기 전에 반드시 이 두 파일을 먼저 읽을 것.

---

## 0. 대전제

원문은 AWS Skill Builder 강의 **대본 추출본**이다. 대본을 그대로 두지 않는다.
**대본 → 이해하기 쉬운 설명본**으로 다시 쓴다. 원문 대본은 남기지 않고 **삭제**한다.

---

## 0-1. 시험 범위의 기준 — AWS 공식 In-Scope 목록

**기준은 날짜가 아니라 AWS가 지금 게시 중인 In-Scope 서비스 목록이다.**

<https://docs.aws.amazon.com/aws-certification/latest/cloud-practitioner-02/clf-02-in-scope-services.html>

시험 가이드는 AWS 공식 문서로 옮겨져 **계속 갱신되는 살아있는 문서**다.
"CLF-C02가 2023-09-19에 출시되었으니 그 시점에서 얼어붙는다"는 전제는 **틀렸다.**
목록 자체에 `이 목록은 완전하지 않으며 변경될 수 있습니다`라고 적혀 있다.

### 판단 규칙

| 상황 | 처리 |
|---|---|
| **In-Scope 목록에 있다** | 확실한 출제 범위. 노트를 만들고 정확히 쓴다 |
| **목록에 없지만 Skill Builder 강의에 나온다** | 삭제하지 않는다. 노트 상단에 **범위 밖 표시**를 달고 우선순위를 낮춘다 |
| **목록에도 강의에도 없다** | 쓰지 않는다 |

강의에 나온다고 곧 출제 범위인 것은 아니다. 강의는 준비 자료이고, 형식적 범위는 공식 목록이다.
다만 목록이 "완전하지 않다"고 스스로 밝히므로, 강의에 나온 것을 지우는 것도 위험하다.

### 서비스 사실관계가 바뀌었을 때

AWS가 서비스 동작·요금을 바꾸면, **시험이 어느 쪽을 묻는지 알 수 없는 구간**이 생긴다.
이럴 때는 한쪽을 고르지 말고 **양쪽을 다 적는다.** 어느 쪽으로 출제되어도 대응할 수 있어야 한다.

확인된 경계 사례:

| 항목 | 사실 | 노트에는 |
|---|---|---|
| 퍼블릭 IPv4 주소 과금 | **발표 2023-07-28** (시험 출시 이전), **시행 2024-02-01** (이후) | 기출의 "붙여 두면 무료·놀리면 과금"을 기본으로 쓰되, 2024-02-01부터 전면 과금으로 바뀐 사실을 함께 적는다 |
| ACM 퍼블릭 인증서 내보내기 | 2025년에 내보내기 옵션 추가 | 기출의 "프라이빗 키를 내보낼 수 없다"를 기본으로 쓴다 |
| Trusted Advisor 범주 | 2023-11에 운영 우수성이 추가되어 6개 | 5개(비용·성능·보안·내결함성·서비스 한도)를 기본으로 쓴다 |

**날짜를 근거로 삼을 때는 반드시 출처를 확인하고, 발표일과 시행일을 구분해서 적는다.**
추측한 날짜를 사실처럼 쓰지 않는다.

## 1. 말투 — 강의하는 사람의 말투

`~합니다` / `~해 보겠습니다` / `~하시기 바랍니다` 체를 쓴다. `~한다` / `~이다` 로 끝내지 않는다.

수동태와 뭉뚱그린 동사를 쓰지 않는다. 아래는 실제로 지적받은 표현들이다.

| 쓰지 말 것 | 이렇게 |
|---|---|
| 커피숍 비유는 계속 **쓰입니다** | 이 비유는 뒤에서도 계속 **사용하겠습니다** |
| 훨씬 쉽게 **잡힙니다** | 한 번씩 **떠올려 보시기 바랍니다** |
| 세 단계로 **이루어집니다** | 세 단계를 **거칩니다** |
| 성격이 잘 **이해됩니다** | 성격을 파악하시는 데 **도움이 됩니다** |
| 훨씬 **오래갑니다** | 이해하시는 편이 **오래 남습니다** |
| 할 일은 하나로 **정리됩니다** | **기억하실 것은 하나입니다** |
| 학습 목표 — ~을 설명**할 수 있습니다** | 이번 강의에서 다룰 내용 — ~을 짚어 **보겠습니다** |
| 앞 모듈까지 오면서 인스턴스를 여러 AZ에 **흩어 놓았습니다** | 여러 AZ에 나누어 두는 방법을 **살펴보았습니다** |

**독자는 아직 아무것도 만들지 않았다.** 강의를 읽었을 뿐이다.
"~을 만들었습니다 / 띄웠습니다 / 배포했습니다"처럼 독자가 실습한 것처럼 쓰지 않는다.
앞 모듈을 되짚을 때는 **"~하는 법을 살펴보았습니다"** 로 쓴다.

## 2. 절대 쓰지 않는 것

- **영상 관련 정보 일체** — 파일명(`LOC_M0x_...mp4`), "다음 동영상에서는", "이번 영상에서는", 영상 대본 접기 블록
- **강사 자기소개, 과정 탐색 안내** 같은 진행용 문구
- 추출 쓰레기 — `#### **Subheading**`, `**** `, `Paragraph text = 17pt, Black - #000000`, `[17pt인지 확인]`, `> 합격 8000%`
- 해설 안에서 보기를 **글자로 지칭하는 것** (`(B)`, `A·C`). 보기 순서는 화면에서 무작위로 섞이므로 전부 틀린 지시가 된다. "요청하는 쪽이 빠졌다"처럼 **내용으로** 지칭한다.

## 3. 이미지

**정보가 있는 이미지만 남긴다.** 판단이 안 서면 Read 도구로 이미지를 직접 열어 보고 결정한다.

- **지운다** — `*_SignPosting_Begin/End.png`(챕터 장식 배너), 아이콘만 나열된 그림, 인물/사물 일러스트, 내가 쓴 표와 내용이 겹치는 그림
- **남긴다** — 구조도, 비교 도표, 아키텍처 다이어그램, 수치가 적힌 그림

경로는 `![](../images/mN/파일명)` 형식을 유지한다.

## 4. 문서 구조 (강의 모듈)

프론트매터(`title`, `tags`, `module`, `status`)는 **그대로 둔다.**

**본문에 `# 제목` 을 쓰지 않는다.** 프론트매터의 `title` 이 곧 페이지 제목이라 중복이 된다.

````markdown
> 강의 N개 · 지식 점검 N문항 · 모듈 평가 N문항

---

## 1. 왜 필요한가

> (원문에 있던 한 줄 인용구를 그대로 유지 — 빌드 스크립트가 이 줄을 파싱한다)

앞 모듈에서 남은 문제를 이어받아, 이번 모듈이 무엇을 푸는지 2~3문단.

## 2. 이번에 새로 나오는 서비스

| 서비스 | 한 줄 | 이 모듈에서 맡는 역할 |
|---|---|---|
| [[서비스-노트-파일명]] | | |

> [!tip] 이번 모듈의 큰 그림
> 서비스들이 서로 어떻게 이어지는지 한 줄.

## 3. 강의 내용

---

### L1. <강의 제목>

> **이번 강의에서 다룰 내용** — ~을 살펴보겠습니다.

#### <소제목>

본문. 표를 적극적으로 쓴다.

```quiz 지식 점검 · <소주제>
...
```

---

### L2. ...

## 4. 모듈 평가

```exam
...
```

## 5. 여기까지의 지도

(```d2 누적 보드. 7-1장 참고)

## 6. 셀프 체크

- [ ] 항목마다 "무엇을 할 수 있으면 되는지" 구체적으로
- [ ] 위 `모듈 평가` N문항을 다시 풀어 전부 맞혔다

확인 문제: [문제 풀이](/aws-clf-c02/quiz) · 틀린 것은 [[wrong-answers]]로.
````

강의 제목은 반드시 `### L1.` `### L2.` … 형식이어야 한다. 빌드 스크립트가 이 패턴으로 강의 수를 센다.

## 5. 문제 문법

`remark-obsidian.mjs` 가 코드 펜스를 인터랙티브 컴포넌트로 바꾼다.

````
```quiz 지식 점검 · 소주제        ← 지식 점검. 고르는 즉시 채점
Q. 질문 텍스트
- 오답 보기
+ 정답 보기
> 해설
```

```exam                          ← 모듈 평가. 전부 풀고 채점 버튼
Q. 1번 문제
- 오답
+ 정답
> 해설

Q. 2번 문제 (2개 선택)
+ 정답 A
+ 정답 B
- 오답
> 해설
```
````

- `+` 를 두 개 이상 쓰면 자동으로 복수 정답 문항이 된다
- 질문·보기·해설 안에서 `**굵게**` 와 `` `코드` `` 가 렌더링된다
- 보기 텍스트는 원문 그대로 살리되 `~입니까 / ~합니다` 체로 다듬는다

**해설은 반드시 직접 쓴다.** 원문 해설은 대부분 `정답입니다! Paragraph text = 17pt` 같은 쓰레기다.
정답인 이유 + **오답이 왜 오답인지** + 가능하면 **문제에서 정답을 고르는 신호**를 쓴다.

## 6. ⚠️ 모듈 평가 정답 키는 깨져 있다

추출 과정에서 `[정답]` 마커가 **거의 항상 1번 보기**에 찍혀 있다. 모듈 1은 8문항 중 6개, 모듈 2는 14문항 중 8개가 틀려 있었다.

**원문 마커를 믿지 말고, 문제를 직접 읽고 정답을 다시 판정한다.** `지식 점검` 쪽 마커는 대체로 맞지만 이것도 검증한다.
`[정답]` 이 두 개 찍혀 있는데 "(2개 선택)"이 아닌 문항도 추출 오류다.

## 7. 포함 관계 시각화 — `layers`

상자 안에 상자로 그린다. 들여쓰기가 곧 중첩이고, 형제가 여러 개면 나란히 배치된다.

````
```layers 캡션 (선택)
AWS 리전 — eu-west-1 | AZ 3개 이상
  가용 영역 A | 독립적인 전력·네트워킹
    데이터 센터 | 중복 전원 · 냉각
  가용 영역 B
    데이터 센터
```
````

`|` 뒤는 옆에 작게 붙는 설명이다. VPC↔서브넷, Organizations↔OU↔계정, 계층형 스택 등에 쓴다.
포함 관계가 아니면 쓰지 말고 표나 ```d2 (7-1장)를 쓴다.

## 7-1. 다이어그램 — `d2`

흐름과 관계를 그린다. [D2](https://d2lang.com) 문법이고, **빌드할 때 SVG 로 구워** 본문에 그대로
심는다. 브라우저로 나가는 자바스크립트는 0 이다. (예전에는 mermaid 를 클라이언트에서 그렸다.)

````
```d2 캡션 (선택)
풀이: "문제 풀이\n/quiz"
노트: 오답노트

풀이 -> 노트: 채점
```
````

- **방향은 기본값(세로)을 쓴다.** `direction: right` 로 눕히면 폭이 두 배가 되고, 좁은 화면에서
  그만큼 더 줄어들어 글자가 안 보인다. 가로가 꼭 필요한 그림만 예외로 둔다.
- 줄바꿈은 `\n`. 라벨에 점·괄호가 있으면 `"..."` 로 묶는다.
- 색은 **정말 강조할 것 하나에만** 준다. 나머지는 테마 기본값을 그대로 둬야 다크모드에서 산다
  (색을 박아 두면 어두운 배경 위에 하얀 덩어리로 남는다).

### 여기까지의 지도 (모듈 노트 4장)

모듈마다 컨테이너를 한 덩이씩 쌓는다. 이번에 새로 배운 모듈에만 `*.class: new` 를 붙인다.

````
```d2
classes: {
  new: {
    style: {
      fill: "#ff9900"
      stroke: "#232f3e"
      stroke-width: 2
      font-color: "#111111"
    }
  }
}

grid-columns: 1
grid-gap: 16

"2. 클라우드 컴퓨팅": {
  grid-rows: 2
  *.width: 190
  *.style.font-size: 12
  *.class: new

  Amazon EC2
  Amazon SQS
}
```
````

`grid-rows` 를 쓴다 (`grid-columns` 아님). D2 의 격자는 `grid-columns` 를 주면 **열 우선**으로
채워서 적어 둔 순서가 흐트러지고, 마지막 열이 짧으면 그 칸들이 세로로 늘어나 상자 크기가
들쭉날쭉해진다. `grid-rows` 는 행 우선이라 순서가 그대로 남고 높이도 균일하다.
값은 **한 줄에 4개**가 되도록 `올림(서비스 수 ÷ 4)` 로 정한다.

## 8. 설명 밀도

- **표를 적극적으로 쓴다.** 특히 비교, 선택지, "문제 속 신호" 컬럼
- 헷갈리는 개념 쌍은 `> [!warning]` 콜아웃으로 갈리는 지점을 명시한다
- 커피숍 비유 등 원문의 비유는 **개념 설명에 필요한 만큼만** 남긴다. 잡담은 버린다
- 콜아웃 종류: `[!tip]`(정리·요령), `[!warning]`(헷갈림 주의), `[!info]`(보충)

## 9. 서비스 노트 (`10-services/`)

프론트매터와 표(카테고리·배우는 모듈·문제은행 출현·상태)는 **그대로 둔다.**
**본문에 `# 제목` 을 쓰지 않는다.** 프론트매터의 `title` 이 곧 페이지 제목이다.
`*(강의 대본 추출 후 채워집니다)*` 같은 자리표시자를 실제 내용으로 채운다.

```markdown
## 한 줄로 말하면

한 문장. (기존 인용구와 같은 내용이면 다듬어서 재사용)

## 핵심 개념

- **용어** — 설명. CLF-C02 수준에서 알아야 할 것만. 4~7개
- 무엇을 하는 서비스인지, 어떤 문제를 푸는지, 특징적인 제약이 무엇인지

## 요금 모델

- 무엇을 기준으로 과금하는지 (요청 수 / 저장 용량 / 실행 시간 / 시간당 …)
- 프리 티어가 있으면 언급. **구체적인 금액은 쓰지 않는다** (자주 바뀐다)
- 요금이 안 붙는 서비스면 "이 서비스 자체는 무료이고, 사용한 리소스 요금만 냅니다"처럼 명시

## 헷갈리는 것과 구분

| 비교 대상 | 차이 |
|---|---|
| [[다른-서비스]] | 한 문장으로 갈리는 지점 |

시험에서 실제로 헷갈리는 서비스만 2~4개. 억지로 채우지 않는다.

## 시험 포인트

- [ ] 문제에 **이런 표현**이 나오면 이 서비스
- [ ] 자주 나오는 함정
```

`## 관련 노트` 와 `## 출처` 는 그대로 둔다. 단 출처의 `*(대본 추출 예정)*` 표시는 지운다.

## 10. 용어집 (`content/glossary.json`)

**기준 독자는 비전공자다.** "서버", "포트", "배포"처럼 업계에서 당연하게 쓰는 말도 모른다고 보고 넣는다.
본문에 실제로 나오는 낱말만 넣되, 나오면 인색하게 굴지 않는다.

- **약어는 풀네임과 함께 등재한다.** `en` 에 풀네임(`EC2` → `Amazon Elastic Compute Cloud`)을 적고,
  **다른 용어 이름 안에 들어간 약어도 그 자리에서 펼친다** — `NAT 게이트웨이` 의 원어는 `NAT Gateway` 가 아니라
  `NAT (Network Address Translation) Gateway` 다. 안 펼치면 `build-glossary.mjs` 가 경고로 알려 준다.
  `long` 에 "왜 이런 약자가 되었는지"를 한 줄 덧붙인다. 그래야 본문에서 **매번 풀어 쓰지 않아도 된다.**
  본문은 `EC2`, `S3`, `ACL` 처럼 짧게 쓰고 뜻은 용어집이 책임진다.
- 카테고리 `서비스 약어` 는 AWS 서비스 이름 약어 전용이다. 일반 IT 약어(HTTP·JSON·SQL)는 주제별 카테고리에 넣는다.
- `short` 는 **한 문장**이다 (호버 카드에 들어간다). 설명이 길어지면 `long` 으로 내린다.
- 같은 표기를 두 용어가 나눠 가질 수 없다. `build-glossary.mjs` 가 중복을 잡아 빌드를 세운다.
- **`see` 는 함께 봐야 뜻이 잡히는 용어를 가리킨다.** 용어집 목록에서는 앵커 링크로, 오른쪽 아래
  상시 사전에서는 눌러서 바로 넘어가는 버튼으로 나온다. 헷갈리는 짝(보안 그룹 ↔ 네트워크 ACL,
  다중 AZ ↔ 읽기 전용 복제본)은 **양쪽 모두에** 서로를 적는다. 없는 용어를 가리키면 빌드가 멈춘다.

### 뜻은 "여러 자리에서 읽힐 것"을 전제로 쓴다

점선은 그 낱말이 **나오는 자리마다** 붙는다. 같은 낱말이 스토리지 꼭지에서도, 보안 꼭지에서도 점선이 된다는 뜻이다.
그래서 한 낱말이 여러 뜻으로 쓰이면 **교재에서 먼저 나오는 뜻**을 `short` 에 두고 나머지를 `long` 에 적어,
어느 꼭지에서 읽어도 말이 되게 해야 한다.

- `온디맨드` — 모듈 1의 클라우드 정의("필요할 때 필요한 만큼")가 먼저다. EC2 요금 옵션은 `long` 으로
- `볼륨` — 저장 공간이 먼저지만 "쿼리 볼륨 · 볼륨 공격"처럼 **양**이라는 뜻도 함께 적는다
- `스택` — CloudFormation 스택과 "스토리지 스택"(층)을 함께 적는다

### 자동 링크에서 조심할 것

`remark-obsidian.mjs` 가 본문 **첫 등장**에만 점선을 붙인다. 그래서 표기를 고를 때 두 가지를 확인한다.

1. **한글 낱말 속에 박히지 않는가.** 플러그인이 "앞 글자가 한글이면 건너뛴다"로 막아 두었지만
   (`템플릿`의 `플릿`, `블로그`의 `로그`, `없앱니다`의 `앱`), 띄어쓰기된 자리는 못 막는다.
2. **다른 뜻으로 쓰이는 자리가 더 많지 않은가.** 예를 들어 `스트리밍` 은 교재에서 대부분
   "애플리케이션 스트리밍"이라 `스트리밍 데이터` 의 별칭으로 두면 뜻이 어긋난다. 이런 표기는 등재하지 않는다.

교재 표기를 그대로 따른다. `경보`(알람 아님), `임곗값`(임계값 아님), `절감형 플랜`(Savings Plans) 처럼
AWS 한국어 문서의 말을 `term` 으로 두고 흔한 다른 표기를 `aliases` 로 받는다.

## 11. 마치고 나서

`npm run build` 로 빌드가 통과하는지 확인한다. (`.next/cache` 를 지울 필요는 없다.)
`dev` 서버가 떠 있어도 플러그인 변경이 없으면 재시작은 필요 없다.
